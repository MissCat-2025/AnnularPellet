父文件夹：命名规则：PC_Contact:Pellet(P)+Clad(C)+接触

子文件夹：
01RossStoute：使用经典的UO2间隙热模型来实现间隙热导计算

一定会与文献的热导的变化存在不一致，原因如下：
1.它是直接从稳态开始计算密实化与热膨胀等应变。这从外间隙尺寸就能判断出来，根据常温下的初始间隙尺寸与它的fig7的间隙尺寸初始值就可看出
2.密实化的最大值的燃耗点对应1-2MWd/kgU，这可能与一般文献不符。
